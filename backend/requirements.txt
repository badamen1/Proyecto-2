# LabClinic Backend - Dependencies
Django==5.2.13
python-dotenv==1.0.1
djangorestframework==3.16.1
djangorestframework_simplejwt==5.5.1
django-cors-headers==4.9.0
django-ratelimit==4.1.0
psycopg2-binary==2.9.11
python-decouple==3.8
PyJWT==2.11.0

# Conexión a FASIL (MySQL on-premise)
# PyMySQL: driver MySQL 100% Python — no requiere compilación de extensiones C
PyMySQL==1.1.1

# HTTP client — usado por fasil_service para proxear PDFs desde servidor BIRT
requests==2.32.3

# Chatbot IA — integración con Gemini 2.5 Flash
google-generativeai>=0.8.0

# Producción — Render
gunicorn==23.0.0
whitenoise==6.9.0
dj-database-url==2.3.0
